
        //--------------------------------------------------------
        swap(x, n);

        gcd(x, n);

        //--------------------------------------------------------

        int arr[n];

        printf("Enter the elements of array of element %d\n ", n);
        for (int i =0; i < n; i++){
                scanf("%d", &x);
                arr1[i] = x;
        }

        sum_arr(n ,arr1);

        //--------------------------------------------------------
        printf("Enter the string\n");
        scanf(" %[^\n]s", str);
        rm_s(str);

-- INSERT --                                                  84,2-9        98%
